# Lumina Studio

Photography portfolio site built with Vite, React, TypeScript, and Tailwind CSS.

## Run

```bash
npm install
npm run dev
```

The app starts on `http://localhost:8080`.

## Build

```bash
npm run build
```
